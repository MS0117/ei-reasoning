# Placeholder package: pyproject's hatch build config ships this directory.
# The controlled-reflection inference code will be copied in later; until then
# this empty package keeps `uv sync` (editable build) working.
